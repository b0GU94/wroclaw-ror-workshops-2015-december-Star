- breadcrumb :payments
-# = link_to t('shared.add_new_item'), , class: 'btn btn-default'
%table.table.table-condensed
  %thead
    %tr
      %th= t('payment.columns.student_id')
      %th= t('payment.columns.date_of_payment')
      %th= t('payment.columns.month')
  %tbody
    = render payments
